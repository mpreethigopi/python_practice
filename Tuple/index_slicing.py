"""2. Tuple Indexing and Slicing
Write a Python program to access the first and last elements of a tuple and create a new tuple that contains only these two elements.

Example:

python
Copy code
# Input: t = (10, 20, 30, 40, 50)
# Output: (10, 50)"""

t = (10, 20, 30, 40, 50)
index=t[0],t[-1]
print(index)